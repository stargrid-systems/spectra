import { describe, expect, it } from "vitest";

// Simple smoke test to keep CI green until real tests are added
describe("smoke", () => {
  it("runs vitest", () => {
    expect(true).toBe(true);
  });
});
